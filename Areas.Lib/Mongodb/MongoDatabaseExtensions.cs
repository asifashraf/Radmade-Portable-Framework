using MongoDB.Driver;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace WebAreas.Lib.Mongodb
{
    public static class MongoDatabaseExtensions
    {
        //public static SafeModeResult Create<T>(this MongoDatabase database,T document)
        //{
        //    return database.GetCollection<T>(typeof(T).Name).Insert<T>(document);
        //}

        //public static MongoCollection<T> Read<T>(this MongoDatabase database)
        //{
        //    return database.GetCollection<T>(typeof(T).Name);
        //}
    }
}
